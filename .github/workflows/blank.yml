name: blank
on:
  push:
jobs:
  blank:
    runs-on: ubuntu-latest
    steps:
      - name: MyCommands
        run: |
          hostname -i
          hostname -I
          nslookup $HOSTNAME
          ifconfig
          ip -4 -br addr
